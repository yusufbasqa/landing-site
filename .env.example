# Base URL of the SubnGo backend API (no trailing slash).
# Local dev: this machine's LAN IP, so it's reachable from phones on the
# same network too (e.g. http://192.168.1.99:8000).
# Production: set this to the real hosted SubnGo API URL.
NEXT_PUBLIC_API_BASE_URL=http://192.168.1.99:8000
