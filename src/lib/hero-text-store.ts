// Mutable, non-reactive position of the hero text block's bottom edge
// (px, measured from the top of the hero section), shared with the R3F
// scene so the mobile cube can be placed just below the text regardless
// of viewport height.
export const heroTextStore = { bottom: 0 };
